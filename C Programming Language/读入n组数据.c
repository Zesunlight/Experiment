#include <stdio.h>

int main()
{
	int total = 0;				//输入total组数据 
	scanf("%d", &total);
	int group[unknown] = {0};	//每组有unknown个数 
	
	for (int i=0; i<total; i++) {
		for (int j=0; j<unknown; j++)
			scanf("%d", &group[j]);	//存储输入的一组数据 
			
		//相关操作
		
		
		printf("%d\n", result);		//输出结果 
	}
	
	return 0;
} 
